<div class="news-item preview">
  <h2 class="item-title"><%= item.title %></h2>
  <small class="item-published_at"><%= item.published_at %></small>
  
  <p class="item-body"><%=%><%= truncate(item.body, :length => 250, :separator => ' ') %></p>
  
  <%=  link_to(item, :class => 'btn more-link') do %>
    <i class="icon-arrow-right"></i>
    <%= I18n.t('ecm.news.item.actions.more') %>
  <% end %>
  <hr />
</div>
